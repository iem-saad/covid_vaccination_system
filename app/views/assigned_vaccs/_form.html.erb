<%= form_with(model: assigned_vacc) do |form| %>
  <% if assigned_vacc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assigned_vacc.errors.count, "error") %> prohibited this assigned_vacc from being saved:</h2>

      <ul>
        <% assigned_vacc.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit "Proceed With Vaccination", class: 'btn btn-success' %>
  </div>
<% end %>
